# Required for default pipeline launch (`prepare train evaluate`), because
# evaluate step generates LLM interpretation and fails without this key.
OPENAI_API_KEY=your_openai_api_key_here

# Optional OpenAI overrides (uncomment when changing model/timeout)
# OPENAI_MODEL=gpt-4o-mini-2024-07-18
# OPENAI_TIMEOUT_SECONDS=15

# Optional data path overrides (uncomment when data files are elsewhere)
# SOURCE_DATA_PATH=backend/data/source.csv
# TRAIN_DATA_PATH=backend/data/train.csv
# TEST_DATA_PATH=backend/data/test.csv

# Optional model artifact path overrides
# TRANSFORMER_PATH=backend/models/feature_transformer.joblib
# Backward-compatible alias used only if TRANSFORMER_PATH is not set
# TRANSFORM_PARAMS_PATH=backend/models/feature_transformer.joblib
# PREDICTOR_DIR=backend/models/ag_insurance

# Optional report output path overrides
# EVALUATION_REPORT_PATH=backend/notebooks/evaluation_report.md
# EDA_REPORT_PATH=backend/notebooks/eda_report.md
# EDA_FIGURES_DIR=backend/notebooks/_eda_figures

# Optional training defaults overrides
# TIME_LIMIT=300
# NUM_BAG_FOLDS=5
# NUM_BAG_SETS=1
