# No environment variables are required to START the backend server.
# It will boot with built-in defaults and read model paths from code defaults.
# Uncomment variables below only when you need custom behavior.

# Required only if you want LLM explanation text in prediction responses.
OPENAI_API_KEY=your_openai_api_key_here

# App metadata overrides (optional)
# APP_NAME=Insurance Pricing API
# APP_VERSION=0.1.0

# Model artifact path overrides (optional; use when artifacts are not in default location)
# MODEL_PATH=backend/models/ag_insurance
# TRANSFORMER_PATH=backend/models/feature_transformer.joblib

# Backward-compatible alias used only if TRANSFORMER_PATH is not set
# TRANSFORM_PARAMS_PATH=backend/models/feature_transformer.joblib

# CORS allowlist override (optional; comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# LLM model/timeout overrides (optional; only relevant with OPENAI_API_KEY)
# OPENAI_MODEL=gpt-4o-mini-2024-07-18
# OPENAI_TIMEOUT_SECONDS=15

# SHAP explanation size override (optional; minimum 1)
# EXPLAIN_TOP_K=8
